# Marketing Website (`landing` mode)

**Label:** `geotrade-website`

This directory is the **GeoTrade marketing website** (hero, stats, world map, system architecture, ML model cards).

## Files

| File | Description |
|------|-------------|
| `LandingPage.tsx` | Main scrollable marketing page |
| `LandingMap.tsx` | Deck.gl country map (hero background + Global Coverage section) |

## Docs & pull instructions

Full manifest, sparse-checkout commands, and run steps:

→ **[`website/README.md`](../../../../website/README.md)** (repo root)

## Open in the app

Top bar → **WEBSITE** tab, or set `mode: 'landing'` in `frontend/src/shared/state/store.ts`.
